[project]
name = "plotting"
version = "0.1.0"
dependencies = [
    "pandas",
    "plotly",
    "koozie",
    "dimes"
]

[tool.uv.sources]
dimes = { git = "https://github.com/bigladder/dimes", branch = "main" }

