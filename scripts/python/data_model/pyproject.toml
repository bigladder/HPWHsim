[project]
name = "data_model"
version = "0.1.0"
dependencies = [
    "cbor2 >= 5.6.5",
    "lattice",
    "jinja2"
]

[tool.uv.sources]
lattice = { git = "https://github.com/bigladder/lattice", branch = "main" }

