[project]
name = "data_model"
version = "0.1.0"
dependencies = [
    "cbor2 >= 5.6.5",
    "jinja2"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["data_model"]

